#define IN 0
#define OUT -1
#define NO_ERROR 0

#include <stdio.h>

int main() {
    
    // create the input string
    
    printf("shellom$ \n");
    
    int flag = IN;
    
    while(flag != OUT) {
     
        // check if it is ls, pwd or exit, and change the flag
        
    }
    
    // read the command from the user
    
    // split the command into arguments
    
    // create child process running the command
    
    // wait for it to finish
    
    return NO_ERROR;
    
}
